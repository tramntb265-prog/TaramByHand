{
  "name": "TaramByHand",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.1"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "codegen": "playwright codegen",

    "test_login_valid": "playwright test --grep @login_valid --project=chromium --headed",
    "test_login_invalid": "playwright test --grep @login_invalid"

  }
}
